# One-shot control installer: attaches the demo deny rule to the OpenClaw agent.
# Stdlib only (urllib), no dependencies.
FROM python:3.12-slim
WORKDIR /app
COPY setup.py .
CMD ["python", "setup.py"]
